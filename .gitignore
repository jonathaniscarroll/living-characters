# Build output — generated by CI, never commit
build/

# Node / npm (if ever added)
node_modules/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Tweego binary — must be committed for CI to work!
# Remove the line below once you have run scripts/vendor-setup.sh
# and are ready to commit the binary.
# vendor/tweego/tweego
